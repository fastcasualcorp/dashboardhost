# ── Supabase ──────────────────────────────────────────────────────────────
# Del dashboard de Supabase: Project Settings → API.
# La anon / publishable key es PÚBLICA (segura en el cliente, la protege la RLS).
# ⚠️ NUNCA pongas aquí (ni en el cliente) la service_role: esa solo va server-side
#    (Edge Functions). Si se filtra, salta toda la seguridad.
VITE_SUPABASE_URL=https://TU-PROYECTO.supabase.co
VITE_SUPABASE_ANON_KEY=tu-anon-key-publica
